# acp-enhanced profile: an ACP automation server exposing the Web-GUI surfaces
# (block-level streaming, usage/stat telemetry, model & reasoning-effort
# switching, permission-preset modes) to ACP clients such as the Zed editor.
#
# NOTE: this file is the reference standalone composition. The official install
# (`dsh plugin --profile acp-enhanced add …`, see README.md) builds the profile
# from the dsh CLI's own `@deepseek-ai/dsh-base` template instead — use that
# flow unless you are deliberately running a hand-rolled composition.
#
# Run with:
#   dsh --profile acp-enhanced
# then register it in Zed settings.json under agent_servers (see README.md).
#
# Stdout carries ONLY newline-delimited ACP JSON-RPC. No logger may write to
# stdout; diagnostics belong on stderr.

# ── the shared agent spine (llm, session, system-prompt, tools, skill,
#    agent registry + loop, goals, jobs) with NO pre-created agents ─────────
- id: spine
  name: '@deepseek-ai/dsh-agent-spine-demo'
  config:
    agents: []
    maxParallelToolCalls: 1
    workspaceContext:
      maxBytes: 65536
    persona: >-
      You are a coding assistant powered by the {{model}} model. Your working
      directory is {{cwd}}. Your bash tool runs under a file sandbox — a
      `[sandbox: file access denied …]` result is policy, not a command bug.

      Verify your work by running the code or tests. Keep answers brief and factual.

# ── the DeepSeek adapter ────────────────────────────────────────────────────
- id: llm-deepseek
  name: '@deepseek-ai/dsh-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max
    models:
      - id: deepseek-v4-flash
      - id: deepseek-v4-pro

# ── sandbox stack: bash + filesystem share one workspace-write policy ───────
- id: sandbox
  name: '@deepseek-ai/dsh-sandbox-local'

- id: sandbox-policy
  name: '@deepseek-ai/dsh-sandbox-policy'
  config:
    mode: !!js "process.env.DSH_PERMISSION_MODE ?? 'workspace-write'"
    workspaceRoot: !!js process.cwd()

- id: subprocess
  name: '@deepseek-ai/dsh-subprocess-local'

- id: bash
  name: '@deepseek-ai/dsh-bash-sandbox'
  config:
    timeoutMs: 60000

- id: approval
  name: '@deepseek-ai/dsh-user-approval'
  config:
    policy: !!js "(process.env.DSH_PERMISSION_MODE ?? 'workspace-write') === 'danger-full-access' ? 'never' : 'ask'"

# ── permission presets: the ACP mode/config surface for sandbox+approval ────
- id: permission-presets
  name: '@deepseek-ai/dsh-permission-presets'
  config:
    presets:
      read-only:
        name: Read-only
        description: Read-only sandbox; every write asks for approval.
        sandbox: read-only
        approval: ask
      workspace-write:
        name: Workspace write
        description: Write inside the workspace; wider retries require approval.
        sandbox: workspace-write
        approval: ask
      danger-full-access:
        name: Full access
        description: Full file access without approval prompts.
        sandbox: danger-full-access
        approval: never

# ── durability: JSONL session log + checkpointing + derived query index ─────
# Session logs live under `$DSH_HOME/sessions/<slug>/<id>/session.jsonl.zstd`
# (the documented layout shared with `dsh web`); DSH_ACP_SESSIONS_ROOT
# overrides the root when an isolated tree is wanted.
- id: session-persistence-jsonl
  name: '@deepseek-ai/dsh-session-persistence-jsonl'
  config:
    root: !!js process.env.DSH_ACP_SESSIONS_ROOT ?? (process.env.DSH_HOME ?? `${process.env.HOME}/.dsh`) + '/sessions'

- id: session-checkpoint-policy
  name: '@deepseek-ai/dsh-session-checkpoint-policy'

- id: session-query-sqlite
  name: '@deepseek-ai/dsh-session-query-sqlite'
  config:
    path: !!js process.env.DSH_ACP_SESSIONS_ROOT ?? (process.env.DSH_HOME ?? `${process.env.HOME}/.dsh`) + '/sessions'
    openAt: startup

- id: session-projection
  name: '@deepseek-ai/dsh-session-projection'

# ── telemetry & compaction ──────────────────────────────────────────────────
# NOTE: `compaction-basic`, `tool-fs` and `tool-todo` below are the host-layer
# rows the bridge's own bundle patch (cordis.patch.yml) disables once the
# package is installed as a bundle — model-facing rows move INTO the
# agent-presets roster (standard/code/minimal/cordis) and a `minimal` session
# must not leak host-layer tools. They stay here so this standalone
# composition works BEFORE the patch applies (plain loader row); expect them
# to read `disabled: true` in `dsh --dump-config` after the bundle install.
- id: token-meter
  name: '@deepseek-ai/dsh-token-meter'

- id: compaction-basic
  name: '@deepseek-ai/dsh-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.08
    maxTokens: 8192
    compactionRetries: 1

# ── filesystem tools on the same sandbox policy ─────────────────────────────
- id: fs-sandbox
  name: '@deepseek-ai/dsh-fs-sandbox'
  config:
    cwd: !!js process.cwd()

- id: fs-observation-policy
  name: '@deepseek-ai/dsh-fs-observation-policy'

- id: tool-fs
  name: '@deepseek-ai/dsh-tool-fs'

# ── remaining model-facing tools ────────────────────────────────────────────
- id: tool-todo
  name: '@deepseek-ai/dsh-tool-todo'
  config:
    allowParallelInProgress: true

- id: repeat-tool-reminder
  name: '@deepseek-ai/dsh-repeat-tool-reminder'

# ── the enhanced ACP transport (stdout is the protocol wire) ────────────────
- id: acp-enhanced
  name: 'dsh-acp-enhanced'
  config:
    provider: deepseek-official
    model: deepseek-v4-flash
